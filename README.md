## Test Tracker Rest Api

### Requirements

- MySQL
    - User named `testTracker` with a password of `testTracker`
    - Database called `test-tracker` that the `testTracker` user has all privileges granted
- Maven3
- Java 1.8
